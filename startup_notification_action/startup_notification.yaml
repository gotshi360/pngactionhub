# Anchorpoint Markup Language
# Full documentation: https://docs.anchorpoint.app/docs/actions/create-actions

version: 1.0
action:
  name: "Startup Notification"

  version: 1
  id: "user::startup_notification"
  category: user
  type: python
  enable: true
  author: "Your Name"
  description: "Shows a configurable toast on every app start (with 'Don't show again today')."

  script: "startup_notification.py"
  settings: "startup_notification_settings.py"

  register:
    sidebar:
      enable: true
