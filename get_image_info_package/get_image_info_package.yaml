version: "1.0"

action:
  name: "Get Image Info Package"
  id: "ap::package::get_image_info"
  type: package
  enable: true
  author: "Anchorpoint Custom"
  description: Extract image and video metadata (dimensions, resolution, bit depth, frame rate, duration, bitrate) from supported files and set them as attributes.

  actions:
    - ap::image::get_info

  settings: get_image_info_settings.py
